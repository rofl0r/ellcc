#!/bin/sh
# toplevel ECC distribution configure

# for now, just fire off default configurations
# in each of the subdirs; users can then go customize
# as desired

mkdir -p llvm-build
cd llvm-build
../llvm/configure --prefix=`pwd`/.. --with-clang-resource-dir=../libecc
