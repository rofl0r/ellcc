VPATH := $(VPATH):$(SRCPATH)/$(OS)

# Startup code.
CRTSRCS = crt0.c crtbegin.c crtend.c

SRCS += on_reset.c on_bootstrap.c

# Target specific sources.
VPATH := $(VPATH):$(SRCPATH)/$(OS)/$(ARCH)
SRCS += start.S _exit.S write.S nmi_handler.S

